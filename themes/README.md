# Themes

Installable PLAIK storefront themes live here.

Themes own presentation only. They may declare assets, layouts, inheritance and supported render hooks through public PLAIK contracts, but must not contain business-domain rules, privileged operations or direct access to Core/private package storage.

The protected final-fallback theme remains part of the `plaik` runtime distribution; additional official themes belong here.
